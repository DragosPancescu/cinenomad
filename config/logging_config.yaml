format: "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
date_format: "%Y-%m-%d %H:%M:%S"

root_level: DEBUG

stdout_level: DEBUG

sqlite_level: DEBUG

# Per-logger level overrides (silences noisy third-party libraries)
logger_overrides:
  PIL: WARNING
  urllib3: WARNING
